services:
 db:
   image: mysql
   container_name: test-db
   ports:
     - "3306:3306"
   environment:
     - MYSQL_ROOT_PASSWORD=password
     - MYSQL_DATABASE=test-db
   volumes:
     - ./test-db:/var/lib/mysql `